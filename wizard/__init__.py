"""Setup wizard for the single-agent project. Run it with `python -m wizard`."""
from pathlib import Path

# Project root: where .env, agent.py and mcp_server.py live.
ROOT = Path(__file__).resolve().parent.parent
